import React from 'react';
import ReactDOM from 'react-dom';
import StarWars from './components/starwars/StarWars';

// it('should call showResults when button is clicked', () => {
//   sinon.spy(StarWars.prototype, 'showResults');
//   const wrapper = mount(<StarWars />);
//   wrapper.find('button').simulate('click');
//   expect(spy).toHaveBeenCalled() //adept assertion to the tool you use
// });

// it('should change button color when clicked', () => {
//   try{
//   const wrapper = mount(<StarWars />);
//   const Button = wrapper.find('button');
//   Button[0].simulate('click');
//   expect(Button[0].length).toBeTruthy();
//   } catch(e){
//     console.log(e);
//   }
//   expect(wrapper.find('button').classList.contains('clicked')).toBeTruthy();
// });
